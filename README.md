# nhop

Client side only substitute for njump.me

```bash
pnpm install
pnpm dev
```

Needs '?' to use:
http://localhost:5173?npub1...

Accepts:

- hex string - converts to `nevent`
- user@domain - converts to `npub`
- `npub` and `naddr` for user profiles
- `note` and `nevent` for notes
